# Layout Analysis

- Page structure is header → main content sections → footer. **CONFIRMED**
- The page runtime uses CSS Grid for the overall master-page rows. **CONFIRMED**
- Form fields use a 12-column grid, with each observed field spanning all 12 columns. **CONFIRMED**
- Hero image source is rendered at 980×915 in the retrieved markup, while its source element occupies its own responsive frame. **CONFIRMED**
- Offer images are delivered at 579×438 for one observed rendition. **CONFIRMED**
- The circular portrait/image is delivered at 350×350. **CONFIRMED**
- Two contact-area media assets are delivered in 482×482 renditions. **CONFIRMED**

Exact page grid column count outside the form, max-width, side padding, alignment coordinates, section padding, and stacking order at each breakpoint: **UNVERIFIED**.
